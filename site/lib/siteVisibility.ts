// Bob confirmed workshops should be closed, including visits via direct links.
// Keep the implementation and CMS content available for a future relaunch.
export const workshopsEnabled = false;
export const workshopsListed = workshopsEnabled;
